let array = []
let a = Number(prompt("Nhap so a"))
let b = Number(prompt("Nhap so b"))
for (let i = 0; i < array.length; i++) {

    for (a = 1; a <= b; a++) {
        console.log("in mang", array.splice(i))
    }
}